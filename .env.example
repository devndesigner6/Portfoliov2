GITHUB_TOKEN=

# Cloudflare R2 (blogs + public assets via cdn.buddhsentripathi.com)
# Used by src/lib/r2Client.ts and scripts/migrate-assets-to-r2.mjs
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT=
R2_ACCOUNT_ID=

# Supabase (blog view counter)
# Required table `views` (slug text primary key, count int) and
# RPC `increment_view_count(post_slug text)` that upserts + increments.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

NEXT_PUBLIC_GA_ID=